# Specify the minimum version for CMake

cmake_minimum_required(VERSION 2.8.10)

# Project's name

project(opennn)

include_directories(${CMAKE_SOURCE_DIR}/openn/opennn)
include_directories(${CMAKE_SOURCE_DIR}/openn/vec)
