# Specify the minimum version for CMake

cmake_minimum_required(VERSION 2.8.10)

# Project's name

project(opennn)

add_directory(${CMAKE_SOURCE_DIR}/opennn/opennn)
add_directory(${CMAKE_SOURCE_DIR}/opennn/vec)
